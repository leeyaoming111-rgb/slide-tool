# Headlines

Headline patterns that open every exhibit slide.

- `kicker/` &mdash; the uppercase, tracked category label above an action title.
- `action-title/` &mdash; the slide headline; states the conclusion, not the category. `bar` and
  `rule` variants.

See `../../docs/CHART_RULES.md` rule 14 and each component's `spec.md`.
